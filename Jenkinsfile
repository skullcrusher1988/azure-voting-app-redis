pipeline {
    agent any

    stages {
        stage('verify branch') {
            steps {
                echo "$GIT_BRANCH"
            }
        }
        stage(('doirrrt'){
           steps{
              sh script: "docker image -a"

           }
        }

    }
}
